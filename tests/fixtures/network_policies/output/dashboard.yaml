dashboard:horizon:Deployment:horizon:
- from:
  - podSelector:
      matchLabels:
        application: app_gateway
        component: proxy
  - podSelector:
      matchLabels:
        application: ingress
        component: server
  ports:
  - port: 4999
    protocol: TCP
