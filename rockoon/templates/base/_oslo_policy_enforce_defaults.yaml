          oslo_policy:
          # NOTE: pin enforce defaults as this will affect customers, API.
          # https://mirantis.jira.com/browse/PRODX-24123
            enforce_new_defaults: false
            enforce_scope: false
