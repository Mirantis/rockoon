{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "lma_deployment"] | namespaces  %}
{%- set prometheus_api = "metric-storage:prometheus:StatefulSet:openstack-prometheus-server" %}
        api:
          spec:
            podSelector:
              matchLabels:
{{ service_childs[prometheus_api].get_pod_labels() | toyaml | indent(16, first=True) }}
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"][prometheus_api] | toyaml | indent(14, first=True) }}
              - from:
{{ from_namespaces(ns_list, service_childs[prometheus_api].get_np_ports(["prometheus"])) | indent(16) }}
