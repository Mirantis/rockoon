{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "lma_deployment"] | namespaces  %}
{%- set aetos_api = "metric-storage:aetos:Deployment:aetos-api" %}
        api:
          spec:
            podSelector:
              matchLabels:
{{ service_childs[aetos_api].get_pod_labels() | toyaml | indent(16, first=True) }}
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"][aetos_api] | toyaml | indent(14, first=True) }}
              - from:
{{ from_namespaces(ns_list, service_childs[aetos_api].get_np_ports(["ae-api"])) | indent(16) }}
