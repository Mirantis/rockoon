parameters:
  glance:
    server:
      message_queue:
        ~members:
        - host: ${_param:mcp2_rabbitmq_glance_address}
        password: ${_param:mcp2_rabbitmq_glance_password}
        user: ${_param:mcp2_rabbitmq_glance_username}
        virtual_host: 'glance'
