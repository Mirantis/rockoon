parameters:
  ironic:
    api:
      message_queue:
        ~members:
        - host: ${_param:mcp2_rabbitmq_ironic_address}
        password: ${_param:mcp2_rabbitmq_ironic_password}
        user: ${_param:mcp2_rabbitmq_ironic_username}
        virtual_host: 'ironic'
